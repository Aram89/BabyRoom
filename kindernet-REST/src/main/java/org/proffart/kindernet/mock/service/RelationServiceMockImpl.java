package org.proffart.kindernet.mock.service;

import org.proffart.kindernet.domain.Relationship;
import org.proffart.kindernet.service.RelationService;
import org.springframework.stereotype.Service;

/**
 * @author Aram Kirakosyan.
 */
@Service
public class RelationServiceMockImpl implements RelationService {
    @Override
    public void addFriend(Relationship friendship) {

    }
}
